<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dopp.doppapi.mapper.sample.SampleMapper">

    <select id="selectSample" resultType="com.dopp.doppapi.dto.sample.SampleDto">
        SELECT st.id, st."name" AS name, st.department
        FROM sample_table st
    </select>

</mapper>